[백준 2960 에라토스테네스의 체](https://www.acmicpc.net/problem/2960)

```python
# 에라토스테네스의 체
# N보다 작거나 같은 모든 소수를 찾는 알고리즘

# 1. 2부터 N까지 모든 정수를 적는다.
# 2. 아직 지우지 않은 수 중 가장 작은 수 P, 이 수는 소수
# 3. P를 지우고, 아직 지우지 않은 P의 배수를 크기 순서대로 지운다.
# 4. 아직 모든 수를 지우지 않았다면, 다시 2번으로 간다.

def eratos(K):
    cnt = 0
    # 배수를 삭제하는 것이기 때문에 remove로 인해 다음 수가 누락되는 것은
    # 신경쓰지 않아도 된다.
    # K 번째 삭제 수 찾기위한 반복문
    while 1:
        # 가장작은 수 P == 리스트의 제일 첫 번째 수
        decimal = number[0]
        for j in number:
            # P의 배수라면 삭제하고 카운트 +1
            if j % decimal == 0:
                number.remove(j)
                cnt += 1
                # K번째 수를 삭제했다면 RETURN해준다.
                if cnt == K:
                    return j


N, K = map(int, input().split())
number = [i for i in range(2,N+1)]
print(eratos(K))1)]

print(eratos(K))
```
